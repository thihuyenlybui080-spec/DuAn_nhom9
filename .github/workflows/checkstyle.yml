name: Code Style Check

# Cấu hình để workflow chạy khi có ai đó push hoặc tạo Pull Request
on:
  push:
    branches: [ "main", "dev" ]
  pull_request:
    branches: [ "main", "dev" ]

jobs:
  checkstyle:
    name: Run Checkstyle
    runs-on: ubuntu-latest

    # THÊM MỚI 1: Cấp quyền cho bot để nó được phép ghi comment vào Pull Request
    permissions:
      checks: write
      pull-requests: write

    steps:
    - name: Checkout Code
      uses: actions/checkout@v4

    - name: Set up JDK 25
      uses: actions/setup-java@v4
      with:
        java-version: '25'
        distribution: 'temurin'
        cache: maven
        
    - name: Execute Checkstyle
      working-directory: ./AuctionSystem_final
      run: mvn checkstyle:check
      
    - name: Checkstyle Reviewer Bot
      uses: jwgmeligmeyling/checkstyle-github-action@master
      if: always() 
      with:
        path: '**/checkstyle-result.xml'
